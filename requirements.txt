# Redrob hackathon — resume-ranker v2
# All packages are standard data-science dependencies with tiny footprint.
# No GPU, no network, no torch, no transformers required.
scikit-learn>=1.3.0,<2.0
numpy>=1.24.0,<3.0